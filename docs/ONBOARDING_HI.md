# फेडरेशन नोड सेटअप गाइड

एग्रीनेट फेडरेशन में आपका स्वागत है! यह गाइड आपको अपने नोड को सेटअप करने, Mycelium के साथ सिंक करने, McEliece कीज़ उत्पन्न करने और UI अनुबंध का उदाहरण प्रदान करने में मार्गदर्शन करेगी।

## पूर्वापेक्षाएं

- Node.js (v14+ अनुशंसित)
- - Git
  - - Yarn या npm
    - - एग्रीनेट रिपोजिटरी तक पहुंच
     
      - ## रिपोजिटरी क्लोन करें
     
      - ```bash
        git clone https://github.com/NTARI-RAND/Agrinet.git
        cd Agrinet
        ```

        ## निर्भरताएं स्थापित करें

        ```bash
        # Yarn का उपयोग करते हुए
        yarn install

        # या npm का उपयोग करते हुए
        npm install
        ```

        ## Mycelium सिंक

        Mycelium फेडरेशन नोड्स को सिंक्रोनाइज़ करने के लिए प्रयुक्त प्रोटोकॉल है।

        ### कॉन्फ़िगरेशन

        प्रोजेक्ट रूट में .env फ़ाइल स्थित करें या बनाएं और जोड़ें:

        ```
        MYCELIUM_NODE_NAME=your-node-name
        MYCELIUM_PEER_URLS=https://peer1.example.com,https://peer2.example.com
        MYCELIUM_PORT=7000
        ```

        ### Mycelium नोड शुरू करें

        ```bash
        yarn mycelium:start
        # या
        npm run mycelium:start
        ```

        ## कुंजी जनरेशन (McEliece)

        McEliece क्रिप्टोसिस्टम फेडरेशन नोड्स के बीच सुरक्षित संचार के लिए उपयोग किया जाता है।

        ### McEliece कुंजियां उत्पन्न करें

        यदि कोई प्रदान की गई स्क्रिप्ट है:

        ```bash
        node scripts/gen-mceliece.js
        ```

        या मैन्युअल रूप से:

        ```javascript
        const mceliece = require('node-mceliece');
        const fs = require('fs');
        const { publicKey, privateKey } = mceliece.keyPair();
        fs.writeFileSync('keys/mceliece_public.key', publicKey);
        fs.writeFileSync('keys/mceliece_private.key', privateKey);
        console.log('McEliece कुंजी जोड़ी /keys/ में उत्पन्न');
        ```

        ## UI अनुबंध उदाहरण

        UI अनुबंध यूजर इंटरफेस और फेडरेशन नोड के API के बीच इंटरैक्शन को परिभाषित करता है।

        ```javascript
        module.exports = {
          authenticateUser: async (publicKey) => {
            // कार्यान्वयन
          },
          requestSync: async () => {
            // कार्यान्वयन
          },
          submitTransaction: async (transaction, signature) => {
            // कार्यान्वयन
          }
        };
        ```

        ## अंतिम चरण

        ✅ अपने फेडरेशन नोड को पूर्ण स्टैक चलाकर और कम से कम एक पीयर से कनेक्शन सत्यापित करके परीक्षण करें।

        🔐 सुनिश्चित करें कि कुंजियां सही तरीके से उत्पन्न और संदर्भित हैं।

        🔗 अपने फ्रंटएंड कोड इंटीग्रेशन में UI अनुबंध विधियां का उपयोग करें।

        अधिक जानकारी के लिए मूल दस्तावेज़ देखें: https://github.com/NTARI-RAND/Agrinet/blob/main/docs/ONBOARDING.md
